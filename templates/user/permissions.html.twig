{% extends 'container.html.twig' %}

{# @var user \App\Entity\User\UserEntity #}

{% block content %}
    <div class="mt-3">
        <div class="row">
            <div class="col-md-12">
                <h2>{{ user.username }} - {{ 'Permissions management'|trans }}</h2>
            </div>
        </div>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
